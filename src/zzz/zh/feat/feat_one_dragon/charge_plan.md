---
lang: zh-CN
title: 体力计划
date: 2026-08-10
---

使用本页说明的功能时，建议阅读以下内容：
::: important

- **体力计划** 配置的是 `实战模拟室`、`区域巡防`、`专业挑战室`、`恶名狩猎 深度追猎`、`合成电池`
- 其中 `恶名狩猎 深度追猎` 属于需要消耗电量的模式，游戏内需要完成周期挑战才会打开入口
- 另外 `恶名狩猎 深度追猎` 与[恶名狩猎](./notorious_hunt.md)里的 `恶名狩猎 周期挑战` 不是同一个模式
:::

## 功能说明

体力计划是「体力刷本」功能的配置页面。体力计划运行前会预读快捷手册资源栏，再按列表顺序执行还没达到 `计划次数` 的计划，直至可用电量不足。

其中 `合成电池` 是个特殊计划：不需要刷本，而是从快捷手册进入合成以太电池，所以没有副本类型、预备编队和自动战斗配置，只需设置 `已运行次数 / 计划次数`

## 配置说明

在「一条龙」页面点击 `体力刷本` 的 ⚙️ 图标进入配置。

1. 循环执行
   - 开启后，除本次已跳过的特训目标外，其余计划均已完成时会重置全部计划。
   - 普通计划的 `已运行次数` 按整轮扣减（减去 `计划次数`，超出部分保留）；已跳过的特训目标则直接清零 `已运行次数`。
2. 跳过计划
   - 开启后，当前计划因电量不足且无法恢复达标时，会依次尝试后续计划。
   - 特训目标已达成、周期挑战还有剩余奖励次数（导致深度追猎无法执行）、单条计划执行失败等情况不受这个开关影响，会在本次运行内临时跳过当前计划，避免卡在同一条计划反复重试。
3. 每日重置
   - 开启后，每天首次运行体力计划前，会将所有体力计划已运行次数清零。
   - 默认关闭。每日重置只影响当天首次运行前的计划进度；不会改变 `循环执行` 的一轮扣减逻辑。
4. 恢复电量
   - 电量不足按所选方式有效补足缺口。
   - 同时使用两种恢复来源时，会先尝试储蓄电量；储蓄电量不足再尝试以太电池。最终仍不足时，按 `跳过计划` 的设置继续后续计划或结束本次运行。
   - 某计划执行时，可用电量=当前电量+恢复电量配置，来源足够才会继续执行。恢复电量会在副本内点 `下一步` 或战斗结束后点 `再来一次` ，触发恢复弹窗后执行。
   - 电量需求：实战模拟室为 `卡片数量 ×20` 点；专业挑战室为 `40` 点；区域巡防和恶名狩猎 深度追猎为 `60` 点；合成电池为 `60` 点。

5. 多倍活动
   - 开启后，自动识别双倍/三倍活动，执行数量取活动剩余次数与当前电量可承担次数的较小值。
   - 分组下可以分别配置 `实战模拟室` 和 `区域巡防` 两张活动卡，各自选好要刷的副本和预备编队。
   - 详细逻辑：
      - 脚本会在识别电量后依次查看 `实战模拟室` 和 `区域巡防`，检查对应栏目是否出现双倍或三倍活动，以及剩余次数。
      - 没有检测到活动、剩余次数为 0 或当前电量不足以执行一次时，会跳过该活动继续看下一个。
      - 命中活动时，会临时优先执行一条活动计划，执行结束后再继续普通体力计划。
      - `实战模拟室` 会在一次挑战里自动选好对应数量的怪物卡，`区域巡防` 则在临时计划里按次数自动点击 `再来一次`。
6. 计划管理
   - 点击 `新增` 添加计划，拖拽可调整执行顺序。
   - 单条计划支持置顶或删除；也可以用 `删除已完成`、`删除所有` 批量清理，误删后可点 `撤销` 恢复。
   - `已运行次数 / 计划次数`：控制本轮还要刷多少次；`计划次数` 填 `0` 时，本轮不会执行这条计划。

## 典型使用方式

**常驻刷养成材料，活动期间优先消耗多倍次数**

1. 开启 `循环执行`、`跳过计划` 和 `每日重置`，让这套计划每天都从 0 次开始，并在某条计划暂时刷不了时继续尝试后续计划。
2. `恢复电量` 选 `不使用` 时，脚本只消耗自然恢复的电量；如果希望自动使用储蓄电量或以太电池，再改成对应选项。
3. 开启 `多倍活动`，配置 `实战模拟室` 或 `区域巡防` 的双倍、三倍活动，并在对应活动卡里选好要刷的副本和编队。
4. 下方计划列表按优先级放入日常要刷的本，例如 `专业挑战室`、`实战模拟室`、`区域巡防` 等。
5. 长期循环使用时，可以把 `计划次数` 设得较大；脚本会受当前电量、恢复电量配置和每日重置共同控制，不会因为次数大就无限刷。

这样脚本会先尝试消耗 `实战模拟室` 或 `区域巡防` 的多倍活动次数，再按计划列表顺序刷日常材料；电量不足且无法恢复达标时，开启 `跳过计划` 会继续尝试后续计划，关闭则会结束本次运行。特训目标已完成、深度追猎暂不满足执行条件、单条计划执行失败等情况不受 `跳过计划` 控制，脚本会临时跳过当前计划，避免卡在同一条计划反复重试。

::: tip
如果体力计划列表为空，或所有计划的 `计划次数` 都是 0，脚本会跳过体力刷本这个步骤。
:::

![体力计划示例](charge_plan/charge_plan_example.png)
